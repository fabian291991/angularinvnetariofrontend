import { Inventarioproducto } from './inventarioproducto.model';

describe('Inventarioproducto', () => {
  it('should create an instance', () => {
    expect(new Inventarioproducto()).toBeTruthy();
  });
});
