export class Usuarios {
    _id?:string; // Identificador único del usuario (opcional)
    seudonimo?:string; // Seudónimo del usuario (opcional)
    correo?:string;  // Correo electrónico del usuario (opcional)
    contrasena?:string; // Contraseña del usuario (opcional)
    token?:string;  // Token de autenticación del usuario (opcional)
}
